# ATEN Web (apps/web)

Next.js PWA frontend for the ATEN Industrial Connections platform. See the repository root
`README.md` for full setup instructions.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Requires the Laravel API (`apps/api`) running and reachable at `NEXT_PUBLIC_API_URL`.
